# HANDOFF — AI Tutor (`edu`) 2026-06-18

**作成日:** 2026-06-18  
**ワークスペース:** `/Volumes/Seagate3TB/projects/edu`  
**状態:** 未コミット差分あり。commit/push は未実行。

---

## 1. 今回進めた内容

### Sprint 29: local uploads -> S3 migration

- `backend/scripts/migrate_uploads_to_s3.py` を追加
- `submission_files.file_path` が `s3://` で始まらない行を対象に、S3 へ upload して DB を `s3://bucket/key` に更新
- 対応 option:
  - `--dry-run`
  - `--limit N`
  - `--continue-on-error`
- S3 key は `S3_UPLOAD_PREFIX/{user_id}/{submission_id}/{filename}`
- 既存 key と衝突する場合は `_1`, `_2` suffix で上書きを避ける
- dry-run は S3 client を作らず、S3 へ接続しない
- local file は削除しない
- `backend/tests/test_migrate_uploads_to_s3.py` を追加
- `docs/infra/production-deploy.md` に移行手順を追加

### E2E / API robustness

- `frontend/src/lib/api.ts`
  - `204` / `205` response では `response.json()` を呼ばず `undefined` を返す
- `frontend/src/__tests__/api.spec.ts` を追加
- curriculum admin E2E の DELETE 後 reload で、後続 GET を待つように修正
  - `frontend/e2e/helpers.ts`
  - `frontend/e2e/admin-curriculum.spec.ts`

### Local E2E isolation

- `make test-e2e` を専用 DB `ai_tutor_e2e` + 一時 backend 起動に変更
- 既存 `ai_tutor` DB を汚さない
- 終了時に backend process と `ai_tutor_e2e` DB を削除
- `.e2e-backend.log` を `.gitignore` に追加
- README / `docs/infra/github-ci-setup.md` を更新

### backend lint debt cleanup

- `make lint` が通るように backend Ruff debt を解消
- `uv run ruff check app scripts tests --fix`
- `uv run ruff check app scripts tests --fix --unsafe-fixes`
- `uv run ruff format app scripts tests`
- 注意: このため backend の差分が広い
- `app.services.submission` は互換 import の窓口だったため、消えた例外 class import を `__all__` 付きで復元
- `app/api/admin/notifications.py` では unsafe fix 後に無意味な recipient lookup query が残っていたため削除
- `tests/test_file_storage.py` では unsafe fix 後に未使用 `full_path` 代入の式だけが残っていたため削除

### Production Docker / Compose

- `backend/Dockerfile`
  - `uv.lock` を copy して依存を lock
  - `alembic.ini`, `alembic/`, `scripts/` を image に含める
  - 本番 compose の `alembic upgrade head` と S3 移行 script が image 内で動く
- `backend/.dockerignore` を追加
- `frontend/.dockerignore` を追加
- `make docker-build` を追加
- `make compose-config` を追加
  - production Compose
  - production Compose + TLS overlay
  - `.env.example` と dummy TLS env で `config --quiet`
- `make terraform-validate` を追加
  - Dockerized Terraform で `fmt -check`
  - read-only mount から `/tmp` に copy して `init -backend=false` + `validate`

### ECS/Fargate runbook

- `docs/infra/ecs-fargate.md` を追加
- Compose service から ECS service/task への対応を記載
- backend / grading-worker / frontend の task 方針
- one-off Alembic migration
- S3 必須化
- deploy order / rollback / readiness checklist
- README / production deploy / ALB deploy からリンク追加

### CI hardening

- `.github/workflows/ci.yml`
  - backend job に Ruff を追加
  - frontend job に `npm run lint` を追加
  - `docker-build` job を追加
    - `make compose-config`
    - backend production image build
    - frontend production image build
  - E2E は `docker-build` 後に実行

### ALB Terraform

- `infra/terraform/alb/variables.tf` に validation を追加
- Dockerized Terraform で `fmt -check` / `validate` 済み

---

## 2. 検証済みコマンド

```bash
make lint
```

Result: passed

```bash
cd backend && uv run pytest
```

Result: `511 passed, 1 skipped`

```bash
cd backend && uv run pytest tests/test_admin_notifications_api.py tests/test_me_notifications_api.py
```

Result: `21 passed`

```bash
cd backend && uv run pytest tests/test_file_storage.py tests/test_admin_user_dashboard_multi_course.py tests/test_course_registry.py
```

Result: `29 passed`

```bash
cd frontend && npm run test
```

Result: `108 passed`

```bash
cd frontend && npm run build
```

Result: passed

```bash
make test-e2e
```

Result: `11 passed`

```bash
make docker-build
```

Result: backend/frontend production image build passed

```bash
make compose-config
```

Result: production Compose / TLS overlay config passed

```bash
make terraform-validate
```

Result: Terraform fmt / init `-backend=false` / validate passed

```bash
docker build -t edu-backend-check:local -f backend/Dockerfile backend
docker run --rm edu-backend-check:local sh -c \
  'test -f /app/alembic.ini && test -d /app/alembic/versions && test -f /app/scripts/migrate_uploads_to_s3.py && uv run alembic --version'
```

Result: `alembic 1.18.4`

```bash
cd frontend && npm audit --audit-level=critical
```

Result: `found 0 vulnerabilities`

```bash
ruby -ryaml -e 'data = YAML.load_file(".github/workflows/ci.yml"); puts data.fetch("jobs").keys.sort.join("\n")'
```

Result:

```text
backend
docker-build
e2e
frontend
```

```bash
git diff --check
```

Result: passed

```bash
docker compose exec -T postgres psql -U postgres -d postgres \
  -c "SELECT datname FROM pg_database WHERE datname LIKE 'ai_tutor_e2e%';"
```

Result: `(0 rows)`

---

## 3. 現在の差分概要

`git diff --stat` 時点:

- `146 files changed`
- `2438 insertions`
- `2013 deletions`

差分が大きい主因:

- backend Ruff format / lint debt cleanup
- Sprint 29 S3 migration script + tests
- E2E isolation
- Docker/CI/deployment docs

未 tracked / untracked:

- `HANDOFF_20260617.md` は古い内容のまま untracked
- `HANDOFF_20260618.md` が今回の正しい引き継ぎ
- `backend/scripts/migrate_uploads_to_s3.py`
- `backend/tests/test_migrate_uploads_to_s3.py`
- `frontend/src/__tests__/api.spec.ts`
- `backend/.dockerignore`
- `frontend/.dockerignore`
- `docs/infra/ecs-fargate.md`

---

## 4. 注意点

- commit/push は未実行
- Terraform は host binary ではなく Dockerized Terraform で検証
- `docker compose -f docker-compose.prod.yml config` を直接実行すると、ローカル `.env` の secret が標準出力へ出る可能性がある。検証には `make compose-config` を使う
- `make docker-build` 実行により local Docker に dangling image と `edu-backend-check:local` が残っている
- backend の広範囲 format はテスト済みだが、レビュー時は「挙動変更」と「format only」を分けて見ること

---

## 5. コミットする場合の推奨分割

1. `feat(storage): add local uploads to s3 migration`
   - `backend/scripts/migrate_uploads_to_s3.py`
   - `backend/tests/test_migrate_uploads_to_s3.py`
   - `docs/infra/production-deploy.md`

2. `test(e2e): isolate local playwright database`
   - `Makefile`
   - `.gitignore`
   - README / CI docs
   - E2E helper/spec changes

3. `fix(frontend): handle empty API responses`
   - `frontend/src/lib/api.ts`
   - `frontend/src/__tests__/api.spec.ts`
   - `frontend/package.json` lint script change

4. `chore(backend): apply ruff cleanup`
   - backend app/tests formatting and lint fixes
   - `backend/app/services/submission.py` re-export restoration

5. `chore(deploy): harden production docker and ci`
   - Dockerfiles / `.dockerignore`
   - `make docker-build`
   - `make compose-config`
   - `make terraform-validate`
   - CI docker-build job
   - ECS/Fargate docs
   - Terraform variable validation

---

## 6. 次にできること

- GitHub Actions を実走して `backend` / `frontend` / `docker-build` / `e2e` を確認
- 広い backend format 差分を review し、必要なら commit 分割前に scope を調整
- GitHub Actions を実走して `backend` / `frontend` / `docker-build` / `e2e` を確認
- production Docker image の起動 smoke test を追加する場合は、test DB を使った一時 compose/one-off task にする
